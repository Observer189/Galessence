using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// Simple movement system used for projectiles and other objects that haven't it's own
/// Engines and things like that
/// </summary>
public class SimpleMovement : MonoBehaviour, IMovementSystem
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public float MaxSpeed { get; }
}
